interface:
  display_name: "Signal Stage Library"
  short_description: "Recover and ingest cinematic story assets"
  default_prompt: "Use $signal-stage-library to recover this thread into a provenance-safe source package, then identify what can advance to Signal Stage ingestion."
